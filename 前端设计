投资工具项目优化版指导文档
引言与目标
本文件是一份面向开发与实施的混合规格文档，融合了产品原型（PRD）、用户体验设计和 API 合同等内容。目标是为团队提供一条清晰的实施路径，使前端和后端能够快速协同开发出一套适用于专业投资者的研究平台。文档采用递进式结构，从全局信息架构出发，逐步落到页面原型、数据流、接口设计、数据模型与性能策略等方面。所有时间均使用 UTC 格式，并注明版本，便于未来升级。
1. 全局信息架构与交互规范
1.1 布局与导航
应用采用三段式工作台布局：左侧为一级导航栏（Sidebar），顶部为全局控制条（TopBar），中间为主内容区（MainContent）。一级导航栏固定五个入口：市场总览、风险情绪、基金组合、单基金研究、情景模拟，每个入口显示图标和中文标题，鼠标悬浮时出现简短说明。不折叠层级、避免复杂树结构，让工具更像“研究台”而非内容后台。
全局控制条包含五个核心控件：
市场选择器（MarketSelector） – 切换数据视角，建议提供 A 股主视角、港股补充视角、全球联动视角。
时间窗口选择器（TimeWindowSelector） – 控制除实时指标外的大部分计算窗口，默认 20D，可选 5D / 20D / 60D / 120D / YTD / 1Y / CUSTOM。
组合选择器（PortfolioSelector） – 在多个用户组合之间切换，也支持“全部组合汇总”。
导出按钮（ExportButton） – 触发当前页面导出，支持 JSON、Markdown、CSV、PNG 四种格式。
研究模式开关（ResearchModeSwitch） – 在 轻量模式 与 研究模式 之间切换：轻量模式折叠次要图表和明细，研究模式展开所有分析组件。
1.2 视觉设计与卡片规范
卡片层级 – 主卡片统一两层结构：第一层显示标题和状态（如“偏高”），第二层显示核心数字或指标，避免在卡片内堆叠长句。解释性文本下沉到“解释层”或右侧抽屉，保证界面简洁。
状态颜色 – 全站使用四种状态颜色：上涨、下跌、中性、警示。禁止引入过多颜色编码系统，保持一致性。
响应式设计 – 页面应兼容常见桌面分辨率，并针对平板设备适配。移动端访问建议仅提供阅读模式或简化版功能。支持深色和浅色主题，以及键盘导航和屏幕阅读器，以满足可访问性要求。
1.3 国际化与个性化
系统初始以简体中文为主，预留国际化能力。所有文案和枚举值都应放在独立的 i18n 文件中，支持英/中等语言切换。支持用户在设置页中选择默认市场视角、时间窗口、导出格式等个性化偏好。
2. 前端技术栈与状态管理
2.1 技术栈建议
推荐前端采用 Next.js + TypeScript 作为框架，结合 Tailwind CSS 或 shadcn/ui 构建 UI，图表部分首选 ECharts（适合热力图、折线、条形、仪表盘、矩阵热图）或 Recharts。请求状态和缓存建议使用 React Query。此组合可提供 SSR/SSG 支持、高度可定制的 UI、稳定的请求管理以及可维护性。
2.2 状态层次
前端状态建议划分三层：
全局筛选状态 – 包含市场视角、时间窗口、当前组合、研究模式等，采用 Zustand 或 React Context 统一管理，支持 URL 同步与本地持久化。
页面数据状态 – 包含异步请求结果（如市场数据、组合详情），使用 React Query 提供缓存、失效重取、错误重试等功能。
局部交互状态 – 如抽屉展开、导入弹窗、导出格式选择等，放在各自组件中使用 useState 或局部状态管理，不与全局共享。
此外，应为每个异步组件实现加载中、错误和空数据三种界面，避免 UI 断崖。建议使用 tsx 泛型约束接口返回类型，并通过自动代码生成工具（如 openapi-typescript）同步后端类型，减少类型不一致风险。
2.3 开发工具与测试
调试与开发体验 – 建议集成 ESLint、Prettier、Husky、lint-staged 实现代码质量控制；使用 Storybook 构建独立组件库；使用 VSCode + Volar / TypeScript 插件提高开发效率。
单元与集成测试 – 推荐使用 Jest 与 React Testing Library 编写单元测试，E2E 测试可采用 Playwright。后端接口的契约测试可使用 schemathesis 或类似工具。
CI/CD – 使用 GitHub Actions 或 GitLab CI 进行自动化构建与测试，部署可使用 Vercel、Netlify 或自建服务器，并配置自动滚动更新。
3. 页面原型与交互细节
3.1 页面骨架与路由
整体路由设计如下：
AppShell ├── Sidebar │   ├── 市场总览 (overview) │   ├── 风险情绪 (sentiment) │   ├── 基金组合 (portfolio) │   ├── 单基金研究 (fund/:fundCode) │   └── 情景模拟 (simulation) ├── TopBar │   ├── MarketSelector │   ├── TimeWindowSelector │   ├── PortfolioSelector │   ├── ExportButton │   └── ResearchModeSwitch └── MainContent     └── CurrentPage
路由包括 /overview、/sentiment、/portfolio、/fund/:fundCode、/simulation、/settings、/import。其中 /settings 和 /import 不在左侧导航栏，需通过顶部按钮或组合页面进入。
3.2 市场总览页
该页回答“今天市场发生了什么”，分三段：
跨市场指数条 – 一行横向滚动或网格小卡片，展示至少九个主要指数（上证指数、深证成指、创业板指、沪深 300、中证 1000、恒生指数、恒生科技、纳斯达克 100、标普 500），每个卡片包含名称、最新点位、涨跌额、涨跌幅、成交额、近 5 日趋势线和更新时间。点击卡片在右侧抽屉弹出更长时间窗口与详细解释。
市场信号层 – 三列布局：
板块轮动卡组：显示最强板块、候选接力板块、高拥挤风险板块，每张卡片含板块名称、综合分数、近 5 日强度、主力净流入、拥挤度标签。
主力资金流卡组：展示行业资金净流入 Top N 和净流出 Top N，支持行业与概念视图切换。
市场广度卡组：展示上涨家数占比、涨停数、跌停数、成交额变化率、市场热度等信息。
市场解释层 – 展示 3–5 条“市场解释卡片”，每条含事件标签、影响市场、证据来源。例：事件标签“海外科技风险偏好修复”，影响市场“港股科技 + A 股算力共振”，证据来源“恒生科技涨幅、主力流入、海外新闻标签”。页底可附 5–8 行自动生成的市场摘要。轻量模式仅保留指数条、最强板块、净流入、三条解释卡；研究模式展开完整板块、广度组件和更多解释。
3.3 风险情绪页
风险情绪页压缩展示市场状态：
双仪表盘 – 上方并排两个仪表盘，左侧为短期综合风险情绪，右侧为中长期综合风险情绪。仪表盘中心显示 0–100 分值，外围标注“极度恐慌、偏恐慌、中性、偏贪婪、过热”五档状态。下方显示关键驱动标签，如“波动率下降”“资金偏好回暖”。
因子拆解卡片 – 下方四张卡片，分别为波动风险、资金偏好、市场广度、海外冲击，每张卡片显示当前得分、变化方向与关键驱动。
时间序列与因子贡献图 – 页底显示双线情绪时间序列（近三个月短期与中长期情绪），及当前情绪分数的因子贡献图。轻量模式隐藏底部图表，仅保留仪表盘与拆解卡；研究模式展开所有内容。
3.4 基金组合页
组合页是核心，分三个标签：组合看穿（overview）、组合诊断（diagnosis）、AI 导出（export）。
3.4.1 组合看穿标签
组合摘要 – 展示总资产、总成本、浮盈浮亏、收益率、基金数量、最大回撤、波动率、更新时间等指标。
资产分布与权重 – 用横向条形图或树图呈现各基金市值占比。
底层暴露 – 三个并列图呈现行业暴露、风格暴露、市场暴露。例如行业暴露显示半导体、医药、消费、银行等，风格暴露显示成长、价值、红利、防御，市场暴露显示 A 股成长、A 股红利、港股科技等。
基金重叠热力图 – 矩阵热图展示基金之间的重仓重叠度或主题重叠度。
目标画像偏差 – 对比用户风险画像与当前组合的偏差。例如当前进攻暴露 71%，而平衡型建议 40–60%；标注偏离程度。
3.4.2 组合诊断标签
风险画像与编辑入口 – 展示用户风险类型（保守、平衡、进攻）及投资周期，可进入设置页编辑。
核心结论卡 – 三个卡片：风险提示、环境适配度、优化方向。风险提示罗列组合中最关键的结构性问题（如集中度、组合重叠）；环境适配度说明当前市场阶段下组合偏进攻还是偏防守；优化方向给出定性建议，例如“减少科技成长集中暴露，增加防御型资产比例”。
支持证据区 – 显示当前市场状态、板块轮动支持和全球事件扰动等证据，作为诊断依据。
3.4.3 AI 导出标签
该标签用于导出结构化结果：
导出格式选择器 – 允许用户选择 JSON、Markdown、CSV 或全部导出；默认选项可在设置页配置。
预览区域 – 展示压缩后的 JSON 与 Markdown 预览；CSV 预览可省略或仅列出头部行。
生成提示词 – 提供 “复制推荐提示词” 按钮，生成适合网页端 AI 解析的提示模板。
异步打包下载 – 图表导出为 PNG 并打包为 ZIP。考虑到导出任务较重，应将导出逻辑放在后端队列，通过轮询或 WebSocket 告知前端完成状态。前端展示导出任务进度，并允许用户稍后在“下载中心”查看。
3.5 单基金研究页
页面应简明扼要：
基金概览条 – 展示基金名称、代码、类型、基金经理、管理规模、成立时间和近 20/60/120 日收益等。
风险收益画像 – 两张核心图：净值曲线与最大回撤曲线，配四个数字卡：波动率、最大回撤、滚动收益排名、与基准偏离。
持仓与风格画像 – 包含行业暴露条形图、重仓股变化表、风格漂移标签、主要指数相关性卡片。
与当前组合关系 – 显示该基金与当前组合的重叠度、增持后将增加的暴露、减持后将降低的暴露。
结论卡组 – 三个字段：适合的持有场景、当前主要优势、当前最大风险。轻量模式仅显示概览条、两张主图和结论字段；研究模式展开重仓股、滚动相关性等细节。
3.6 情景模拟页
情景模拟页用于估算组合的未来分布和情景敏感度，划分为参数区和结果区：
参数与情景选择 – 左侧可选择模拟模式：统计模拟（Statistical）或情景冲击（Scenario）。统计模拟允许设置模拟期限、模拟路径数、置信区间，并选择是否使用 bootstrap 采样；情景冲击提供若干预设场景（如港股科技回撤、半导体修复、医药反弹、全球风险偏好下降、美元走强），用户也可自定义场景。右侧展示当前组合摘要和风险画像，提醒用户模拟对象。
结果展示 – 保留三块核心内容：
收益分布热力图 – 展示未来不同时间点（如 10D、30D、60D）的收益概率分布区间。
理论最优/最差回报曲线 – 折线图展示不同时间点的理论最大收益与理论最大损失对应路径。
情景敏感度表 – 按场景列出组合预期收益变化、损失风险和受影响最大暴露项。结果顶部可单独显示预计最大回撤数值。
模拟计算属于高耗时任务，应在后端异步执行并缓存短期结果。前端只负责触发模拟请求并呈现数据，不自行计算金融指标。
3.7 导入与设置页面
3.7.1 导入页
导入是重要功能，支持三种模式：
基金代码导入 – 用户手动输入基金代码列表，提供当前份额、市值与总成本。表单需校验基金代码格式并支持批量输入。
截图导入 – 用户上传一张或多张持仓截图，系统通过 OCR 识别基金名称、代码、市值、持仓份额等信息并生成预览。由于 OCR 可能存在误识别，前端应提供可编辑的表格让用户确认并补充缺失字段。
CSV 导入 – 用户下载模板并上传交易记录或当前持仓 CSV。系统解析后返回字段映射预览，用户可以调整列对应关系。
导入完成后进入“校准页”，处理成本与历史交易不完整的问题，例如缺失成本、拆分/合并份额、认购费用等。校准完成后创建新的组合并返回 portfolio_id。
3.7.2 设置页
设置页包含三类设置：
风险画像设置 – 用户选择保守型、平衡型或进攻型风险画像，并设定投资周期。系统可引导用户完成风险测评问卷，以量化风险容忍度。
默认视角设置 – 设置默认市场视角、默认时间窗口、默认研究模式等，影响应用启动后的默认筛选值。
导出与数据偏好设置 – 定义默认导出格式、是否导出图表图片、是否在组合分析中启用全球事件扰动因子等。
所有设置应存储在后端并在前端持久化缓存。修改偏好后，相关组件应实时更新。
4. 后端总体架构与 API
4.1 架构建议
后端推荐基于 FastAPI + Pydantic + SQLAlchemy 实现 REST API；数据库初期使用轻量级 DuckDB/Parquet 存储，后期可升级为 PostgreSQL。缓存层可使用 Redis，消息队列可使用 RabbitMQ 或 Celery，用于处理异步任务（OCR、情景模拟、导出打包等）。统一日志与监控可使用 Prometheus + Grafana 或企业级 APM 工具。
后端分四层：
数据接入层 – 负责从外部源（AKShare、Tushare、新闻源、OCR 模块）拉取数据，定期更新并缓存，注意遵循数据供应商的许可协议。
分析层 – 负责计算市场状态、情绪分数、组合穿透、基金分析、情景模拟等核心逻辑。建议使用 Python 科学计算库（NumPy、Pandas、SciPy）编写算法模块，并对核心算法进行单元测试和性能评估。
服务层 – 将分析结果聚合成前端所需的结构化数据，提供统一的 API 接口和错误处理逻辑。
导出层 – 负责生成 JSON、Markdown、CSV 文件及 PNG 图表，建议异步执行并缓存结果。
确保业务逻辑全部在后台完成，前端不自行计算金融指标，以避免页面口径不一致的问题。
4.2 API 设计
统一接口路径以 /api/v1/ 开头，并按资源分组：
/api/v1/system/*      // 系统与初始化 /api/v1/market/*      // 市场总览相关 /api/v1/sentiment/*   // 风险情绪相关 /api/v1/portfolio/*   // 组合相关 /api/v1/fund/*        // 单基金研究 /api/v1/simulation/*  // 情景模拟 /api/v1/import/*      // 导入与 OCR /api/v1/export/*      // 导出 /api/v1/settings/*    // 设置与偏好
响应格式统一：
{   "success": true,   "message": "ok",   "data": {},   "meta": {     "timestamp": "2026-04-14T12:30:00Z",     "version": "v1"   } }
错误响应应包含明确的 error_code，便于前端处理。
4.2.1 系统接口
GET /api/v1/system/bootstrap – 应用启动时一次性拉取基础配置。返回支持的市场视角、时间窗口、研究模式、用户组合列表及当前默认设置。
4.2.2 市场总览接口
GET /api/v1/market/overview – 根据 market_view 和 time_window 返回跨市场指数条、板块轮动、资金流、市场广度、市场解释以及当日摘要。建议支持可选字段参数以减小响应体。
子接口：/api/v1/market/indices、/sector-rotation、/fund-flows、/breadth、/explanations 用于按需刷新部分区域。
4.2.3 风险情绪接口
GET /api/v1/sentiment/overview – 根据 market_view、time_window 返回短期和中长期情绪分数、因子拆解、时间序列和贡献列表。
4.2.4 组合接口
GET /api/v1/portfolio/{portfolio_id}/overview – 返回组合看穿页数据，包括组合摘要、基金权重、行业/风格/市场暴露、基金重叠矩阵和目标画像偏差。
GET /api/v1/portfolio/{portfolio_id}/diagnosis – 返回组合诊断结论，包括风险画像、风险提示、环境适配度、优化建议和支持证据。
GET /api/v1/portfolio/{portfolio_id}/export-pack – 根据 format 生成 AI 导出包，可返回预览或下载链接。此任务应异步执行，返回任务 task_id，供前端查询。
4.2.5 单基金研究接口
GET /api/v1/fund/{fund_code}/overview – 获取基金基础信息，如名称、类型、基金经理、管理规模、成立时间以及近 20/60/120 日收益。
GET /api/v1/fund/{fund_code}/analysis – 获取风险收益、持仓风格、与当前组合关系和结论。
4.2.6 情景模拟接口
POST /api/v1/simulation/run – 根据 mode=statistical 或 mode=scenario 启动模拟。统计模式需提供模拟期限、路径数、置信区间、采样方法；情景模式需提供 scenario_ids。响应包含收益分布、最优/最差曲线、情景敏感度等。该接口应返回 task_id，前端通过轮询查询完成状态。
4.2.7 导入与 OCR 接口
POST /api/v1/import/screenshot – 上传截图文件（多张），返回 OCR 初识别结果及置信度，并给出警告提示。
POST /api/v1/import/csv – 上传 CSV 文件，返回解析结果和字段映射预览。
POST /api/v1/import/confirm – 用户确认并补充识别结果，生成组合，返回 portfolio_id。
4.2.8 导出接口
POST /api/v1/export/page – 提交导出任务，指定页面（如 portfolio）、组合 ID 和导出格式列表。返回文件下载地址或任务 ID。建议导出任务在后台队列运行，完成后将下载链接推送给用户。
4.2.9 设置接口
GET/PUT /api/v1/settings/profile – 获取或更新风险画像设置，包括风险类型、投资周期、首选防御比例和进攻比例。
GET/PUT /api/v1/settings/preferences – 获取或更新偏好设置，如默认市场视角、时间窗口、研究模式、默认导出格式等。
4.3 核心数据模型
逻辑模型应覆盖以下实体，各字段可据实际业务扩充：

除上述实体外，可视业务需要设计 SentimentSnapshot、ScenarioDefinition 等模型。推荐所有枚举值存放在单独文件并在后端和前端共享，减少魔法字符串。
5. 计算逻辑与前后端协作边界
某些逻辑必须在后端计算完成，前端仅负责展示：
指数涨跌、资金流、板块轮动、市场广度、情绪分数等市场指标。
组合暴露、基金重叠矩阵、情景模拟等涉及复杂金融运算的结果。
OCR 识别、CSV 解析 需在后端进行，并返回可编辑结果供前端校正。
前端可以进行的轻计算包括：UI 排序、过滤、字符串格式化、百分比转换以及图表美化等。严禁在前端编写统计公式，防止口径不统一。
6. 缓存与更新策略
为了平衡性能与时效性，各类数据应设定合理的刷新频率：

可在后端使用 Redis 或 DuckDB 缓存热点数据；对于市场总览和风险情绪等高访问接口，建议生成快照供多用户复用。组合分析等以用户为维度的数据则按组合 ID 缓存。
7. 实施注意事项与优化建议
7.1 数据来源与许可
外部数据源（如 AKShare、Tushare、新闻 API）须符合使用协议。建议与数据提供方达成正式授权，并在后端设置缓冲层，避免直接向第三方频繁请求。
7.2 安全与鉴权
身份认证与授权 – 应支持 OAuth2 或 JWT，前端在请求时携带访问令牌。某些接口（如导入、设置）需绑定用户身份，必须验证权限。
数据加密 – HTTPS 全站加密传输；服务器配置证书并使用 HSTS。敏感信息（如用户组合价值、成本）在数据库中加密存储。
防滥用与速率限制 – 对单用户的 API 请求设置速率限制，防止恶意爬取。对导出、模拟等耗时操作采用任务队列，限制并发。
7.3 国际化与地域支持
在 API 设计中尽量使用英文字段，并在前端根据用户语言切换展示文案。时间数据统一返回 ISO8601 格式，并注明时区。在未来支持海外市场时，可增加港股、美股、A 股等市场的指数列表，并根据本地法律调整数据展示。
7.4 性能与扩展
水平扩展 – 后端应设计为无状态服务，便于横向扩展；长时间任务放入队列，避免阻塞 Web 服务器。
异步与事件驱动 – 利用 Celery 等任务队列处理 OCR、导出和情景模拟。可通过 WebSocket 或 Server-Sent Events 向前端推送任务进度。
数据库设计 – 选择合适的索引和分区策略，提高组合持仓、历史行情查询速度。对于历史行情大量读写，可将热点数据放入列式存储（如 Parquet + DuckDB），冷数据归档。
7.5 测试与监控
编写单元测试覆盖关键计算逻辑，尤其是情景模拟和组合暴露算法。
部署监控与告警系统，跟踪 API 延迟、错误率、系统负载等指标。
对接日志系统，集中处理应用日志、访问日志以及异常日志，保证快速定位问题。
7.6 用户体验优化
在导出和模拟操作中加入进度反馈，避免用户无响应感。
在重交互页面（如组合和单基金研究）中提供跳转锚点或目录，方便用户快速定位不同模块。
考虑增加搜索与筛选功能，例如基金列表筛选、指数列表搜索等。
针对长时间操作（情景模拟、导出）可提供后台提醒或邮件通知。
7.7 潜在优化方向
GraphQL 或 RPC – 在未来版本可以考虑引入 GraphQL 或 gRPC 简化接口管理，并实现按需查询与实时订阅。
多租户架构 – 未来若需要支持机构级多个用户共享数据，可在模型中增加租户字段并进行权限隔离。
定制化报告 – 除标准导出外，可为专业用户提供定制化报告模板，支持自定义图表组合和指标选择。
结语
本文档旨在提供一份结构完整、便于实施的投资工具项目指导方案。通过明确界面原型、技术栈、数据结构、接口设计、缓存策略以及实施注意事项，团队可以减少反复沟通，加速交付周期。在实际开发过程中，应结合实际情况调整优先级，先实现最小可行产品（MVP），再逐步迭代完善。欢迎开发者和产品人员依据本文档进一步补充细节，共同打造高质量的投资研究工具。
